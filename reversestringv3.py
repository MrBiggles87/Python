def reversestring(message):
	message=input("Please enter a message: ")
    newstring = " "
    index=len(message)
    	while index:
    		index-=1                    
    		newstring += a_string[index]
   		return newstring

